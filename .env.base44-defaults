# Placeholder defaults so the app boots before real Supabase credentials arrive.
# Overridden by /run/base44/app.env (platform-managed secrets), which is listed
# as the LAST env_file entry in docker-compose.base44.yml.
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder
